diff --git a/node_modules/@smogon/calc/dist/adaptable.d.ts b/node_modules/@smogon/calc/dist/adaptable.d.ts
index c5323b9..01b5ee7 100644
--- a/node_modules/@smogon/calc/dist/adaptable.d.ts
+++ b/node_modules/@smogon/calc/dist/adaptable.d.ts
@@ -1,6 +1,7 @@
 export { calculate } from './calc';
-export { Pokemon } from './pokemon';
-export { Move } from './move';
 export { Field, Side } from './field';
+export { Move } from './move';
+export { Pokemon, PokemonOptions } from './pokemon';
 export { Result } from './result';
 export { Stats } from './stats';
+
diff --git a/node_modules/@smogon/calc/dist/data/species.js b/node_modules/@smogon/calc/dist/data/species.js
index bc38c31..bebc176 100644
--- a/node_modules/@smogon/calc/dist/data/species.js
+++ b/node_modules/@smogon/calc/dist/data/species.js
@@ -8781,9 +8781,7 @@ var SS_PATCH = {
         baseSpecies: 'Zigzagoon'
     }
 };
-var SS = (0, util_1.extend)(true, {}, SM, SS_PATCH);
-delete SS['Pikachu-Starter'];
-delete SS['Eevee-Starter'];
+
 var PLA_PATCH = {
     Arcanine: { otherFormes: ['Arcanine-Hisui'] },
     Avalugg: { otherFormes: ['Avalugg-Hisui'] },
@@ -9009,6 +9007,9 @@ var PLA_PATCH = {
         nfe: true
     }
 };
+var SS = (0, util_1.extend)(true, {}, SM, SS_PATCH, PLA_PATCH);
+delete SS['Pikachu-Starter'];
+delete SS['Eevee-Starter'];
 var SV_PATCH = {
     Bisharp: { nfe: true },
     Cresselia: { bs: { df: 110, sd: 120 } },
diff --git a/node_modules/@smogon/calc/dist/field.d.ts b/node_modules/@smogon/calc/dist/field.d.ts
index 2faa6d5..32eeaef 100644
--- a/node_modules/@smogon/calc/dist/field.d.ts
+++ b/node_modules/@smogon/calc/dist/field.d.ts
@@ -1,11 +1,12 @@
+import type { GameType, Terrain, Weather } from './data/interface';
 import type { State } from './state';
-import type { GameType, Weather, Terrain } from './data/interface';
 export declare class Field implements State.Field {
     gameType: GameType;
     weather?: Weather;
     terrain?: Terrain;
     isMagicRoom: boolean;
     isWonderRoom: boolean;
+    isTrickRoom: boolean;
     isGravity: boolean;
     isAuraBreak?: boolean;
     isFairyAura?: boolean;
@@ -30,6 +31,7 @@ export declare class Side implements State.Side {
     cannonade: boolean;
     volcalith: boolean;
     isSR: boolean;
+    isStickyWebs: boolean;
     isReflect: boolean;
     isLightScreen: boolean;
     isProtected: boolean;
diff --git a/node_modules/@smogon/calc/dist/field.js b/node_modules/@smogon/calc/dist/field.js
index 78803e4..01673b4 100644
--- a/node_modules/@smogon/calc/dist/field.js
+++ b/node_modules/@smogon/calc/dist/field.js
@@ -24,6 +24,7 @@ var Field = (function () {
         this.terrain = field.terrain;
         this.weather = field.weather;
         this.isMagicRoom = !!field.isMagicRoom;
+        this.isTrickRoom = !!field.isTrickRoom;
         this.isWonderRoom = !!field.isWonderRoom;
         this.isGravity = !!field.isGravity;
         this.isAuraBreak = field.isAuraBreak || false;
@@ -62,6 +63,7 @@ var Field = (function () {
             terrain: this.terrain,
             isMagicRoom: this.isMagicRoom,
             isWonderRoom: this.isWonderRoom,
+            isTrickRoom: this.isTrickRoom,
             isGravity: this.isGravity,
             attackerSide: this.attackerSide,
             defenderSide: this.defenderSide,
@@ -87,6 +89,7 @@ var Side = (function () {
         this.cannonade = !!side.cannonade;
         this.volcalith = !!side.volcalith;
         this.isSR = !!side.isSR;
+        this.isStickyWebs = !!side.isStickyWebs;
         this.isReflect = !!side.isReflect;
         this.isLightScreen = !!side.isLightScreen;
         this.isProtected = !!side.isProtected;
diff --git a/node_modules/@smogon/calc/dist/index.d.ts b/node_modules/@smogon/calc/dist/index.d.ts
index 0e1870a..f411366 100644
--- a/node_modules/@smogon/calc/dist/index.d.ts
+++ b/node_modules/@smogon/calc/dist/index.d.ts
@@ -1,6 +1,6 @@
-import type { State } from './state';
-import type * as I from './data/interface';
 import * as A from './adaptable';
+import type * as I from './data/interface';
+import type { State } from './state';
 export declare function calculate(gen: I.GenerationNum | I.Generation, attacker: A.Pokemon, defender: A.Pokemon, move: A.Move, field?: A.Field): A.Result;
 export declare class Move extends A.Move {
     constructor(gen: I.GenerationNum | I.Generation, name: string, options?: Partial<Omit<State.Move, 'ability' | 'item' | 'species'>> & {
@@ -29,16 +29,18 @@ export declare class Pokemon extends A.Pokemon {
     static getForme(gen: I.GenerationNum | I.Generation, speciesName: string, item?: string, moveName?: string): string;
 }
 export declare function calcStat(gen: I.GenerationNum | I.Generation, stat: I.StatID | 'spc', base: number, iv: number, ev: number, level: number, nature?: string): number;
-export { Field, Side } from './field';
-export { Result } from './result';
-export { type GenerationNum, type StatsTable, type StatID } from './data/interface';
-export { Generations } from './data/index';
-export { toID } from './util';
-export { type State } from './state';
 export { ABILITIES } from './data/abilities';
+export { Generations } from './data/index';
+export { type GenerationNum, type StatID, type StatsTable } from './data/interface';
 export { ITEMS, MEGA_STONES } from './data/items';
 export { MOVES } from './data/moves';
-export { SPECIES } from './data/species';
 export { NATURES } from './data/natures';
+export { SPECIES } from './data/species';
 export { TYPE_CHART } from './data/types';
+export { Field, Side } from './field';
+export { PokemonOptions } from './pokemon';
+export { Result } from './result';
+export { type State } from './state';
 export { STATS, Stats } from './stats';
+export { toID } from './util';
+
diff --git a/node_modules/@smogon/calc/dist/pokemon.d.ts b/node_modules/@smogon/calc/dist/pokemon.d.ts
index b186309..a112998 100644
--- a/node_modules/@smogon/calc/dist/pokemon.d.ts
+++ b/node_modules/@smogon/calc/dist/pokemon.d.ts
@@ -1,6 +1,21 @@
 import type * as I from './data/interface';
 import type { State } from './state';
+export type PokemonOptions = Partial<State.Pokemon> & {
+    curHP?: number;
+    id?: string;
+    ivs?: Partial<I.StatsTable> & {
+        spc?: number;
+    };
+    evs?: Partial<I.StatsTable> & {
+        spc?: number;
+    };
+    boosts?: Partial<I.StatsTable> & {
+        spc?: number;
+    };
+    types?: [I.TypeName] | [I.TypeName, I.TypeName];
+};
 export declare class Pokemon implements State.Pokemon {
+    id: string;
     gen: I.Generation;
     name: I.SpeciesName;
     species: I.Specie;
@@ -27,18 +42,7 @@ export declare class Pokemon implements State.Pokemon {
     status: I.StatusName | '';
     toxicCounter: number;
     moves: I.MoveName[];
-    constructor(gen: I.Generation, name: string, options?: Partial<State.Pokemon> & {
-        curHP?: number;
-        ivs?: Partial<I.StatsTable> & {
-            spc?: number;
-        };
-        evs?: Partial<I.StatsTable> & {
-            spc?: number;
-        };
-        boosts?: Partial<I.StatsTable> & {
-            spc?: number;
-        };
-    });
+    constructor(gen: I.Generation, name: string, options?: PokemonOptions);
     maxHP(original?: boolean): number;
     curHP(original?: boolean): number;
     hasAbility(...abilities: string[]): boolean;
@@ -47,7 +51,8 @@ export declare class Pokemon implements State.Pokemon {
     hasType(...types: I.TypeName[]): boolean;
     hasOriginalType(...types: I.TypeName[]): boolean;
     named(...names: string[]): boolean;
-    clone(): Pokemon;
+    equals(other: Pokemon): boolean;
+    clone(options?: PokemonOptions): Pokemon;
     private calcStat;
     static getForme(gen: I.Generation, speciesName: string, item?: I.ItemName, moveName?: I.MoveName): string;
     private static withDefault;
diff --git a/node_modules/@smogon/calc/dist/pokemon.js b/node_modules/@smogon/calc/dist/pokemon.js
index 95a17fa..8f75ead 100644
--- a/node_modules/@smogon/calc/dist/pokemon.js
+++ b/node_modules/@smogon/calc/dist/pokemon.js
@@ -35,7 +35,8 @@ var Pokemon = (function () {
         this.species = (0, util_1.extend)(true, {}, gen.species.get((0, util_1.toID)(name)), options.overrides);
         this.gen = gen;
         this.name = options.name || name;
-        this.types = this.species.types;
+        this.id = options.id || (this.name + ':' + Math.random());
+        this.types = options.types ? options.types : this.species.types;
         this.weightkg = this.species.weightkg;
         this.level = options.level || 100;
         this.gender = options.gender || this.species.gender || 'M';
@@ -80,8 +81,9 @@ var Pokemon = (function () {
             }
             finally { if (e_1) throw e_1.error; }
         }
-        var curHP = options.curHP || options.originalCurHP;
-        this.originalCurHP = curHP && curHP <= this.rawStats.hp ? curHP : this.rawStats.hp;
+        const curHP = options.curHP !== undefined ? options.curHP
+            : options.originalCurHP !== undefined ? options.originalCurHP : undefined;
+        this.originalCurHP = curHP !== undefined && curHP <= this.rawStats.hp ? curHP : this.rawStats.hp;
         this.status = options.status || '';
         this.toxicCounter = options.toxicCounter || 0;
         this.moves = options.moves || [];
@@ -174,8 +176,11 @@ var Pokemon = (function () {
         }
         return names.includes(this.name);
     };
-    Pokemon.prototype.clone = function () {
-        return new Pokemon(this.gen, this.name, {
+    Pokemon.prototype.equals = function (other) {
+        return this.id === other.id;
+    };
+    Pokemon.prototype.clone = function (options) {
+        return new Pokemon(this.gen, this.name, __assign({
             level: this.level,
             ability: this.ability,
             abilityOn: this.abilityOn,
@@ -183,6 +188,7 @@ var Pokemon = (function () {
             dynamaxLevel: this.dynamaxLevel,
             alliesFainted: this.alliesFainted,
             boostedStat: this.boostedStat,
+            id: this.id,
             item: this.item,
             gender: this.gender,
             nature: this.nature,
@@ -193,9 +199,10 @@ var Pokemon = (function () {
             status: this.status,
             teraType: this.teraType,
             toxicCounter: this.toxicCounter,
+            types: this.types,
             moves: this.moves.slice(),
             overrides: this.species
-        });
+        }, options));
     };
     Pokemon.prototype.calcStat = function (gen, stat) {
         return stats_1.Stats.calcStat(gen, stat, this.species.baseStats[stat], this.ivs[stat], this.evs[stat], this.level, this.nature);
diff --git a/node_modules/@smogon/calc/dist/state.d.ts b/node_modules/@smogon/calc/dist/state.d.ts
index 16d8644..dda4ae4 100644
--- a/node_modules/@smogon/calc/dist/state.d.ts
+++ b/node_modules/@smogon/calc/dist/state.d.ts
@@ -39,6 +39,7 @@ export declare namespace State {
         terrain?: I.Terrain;
         isMagicRoom?: boolean;
         isWonderRoom?: boolean;
+        isTrickRoom?: boolean;
         isGravity?: boolean;
         isAuraBreak?: boolean;
         isFairyAura?: boolean;
@@ -58,6 +59,7 @@ export declare namespace State {
         cannonade?: boolean;
         volcalith?: boolean;
         isSR?: boolean;
+        isStickyWebs?: boolean;
         isReflect?: boolean;
         isLightScreen?: boolean;
         isProtected?: boolean;
