/**
 * Styles for DeveloperTools component
 */

import { makeStyles } from '@fluentui/react-components';

export const useStyles = makeStyles({
  container: {
    padding: '1em',
  },
  exportButton: {
    marginTop: '1em',
  },
  debugInfo: {
    marginTop: '1em',
    fontSize: '0.9em',
    color: '#666',
  },
});
