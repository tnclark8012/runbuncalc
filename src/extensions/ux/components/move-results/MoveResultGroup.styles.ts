/**
 * Styles for MoveResultGroup component
 */

import { makeStyles } from '@fluentui/react-components';

export const useStyles = makeStyles({
  moveLabel: {
    display: 'flex',
    justifyContent: 'space-between',
    width: '100%',
  },
  damagePercent: {
    marginLeft: '1rem',
  },
  fasterContainer: {
    border: '2px solid #0078d4',
    borderRadius: '4px',
    padding: '8px',
  },
});
